<% if params[:refresh] == "true" %>

	<% if params[:line_no].include? "table_" %>
		<% table_no = params[:line_no].split("_")[1] %>

		$("#board<%= table_no %>").html("<%= escape_javascript(render 'table', :table_no=>table_no.to_i, :row_per_table=>@row_per_table, :boards=>@boards.order('no ASC') ) %>");

	<%# elsif params[:line_no] == "table2" %>

		//$("#board2").html("<%#= escape_javascript(render 'table2') %>");

	<% elsif params[:line_no].include? "image_" %>
	
	

	<% elsif params[:line_no].to_i != 0 %>
		<%# key = @big_data.keys[params[:line_no].to_i-1] %>
		<%# value = @big_data.values[params[:line_no].to_i-1] %>
		<%# new_value = {key => value} %>
		$("#detail_graph_<%= params[:line_no] %>").html("<%= escape_javascript(render 'detail_graph',:big => @big_data.first) %>");
	<% end %>
	<%# @big_data = @big_data.first %>
	
	<%# @big_data.each_with_index do |big_dat, index| %>
		//$("#detail_graph_<%#= big_dat.first %>").html("<%#= escape_javascript(render 'detail_graph',:big => big_dat) %>");
    <%# end %>


<% end %>
