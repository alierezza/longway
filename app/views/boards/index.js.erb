<% if params[:refresh] == "true" %>
	<%# @boards = Line.all %>
	$("#board").html("<%= escape_javascript(render 'table') %>");
	$("#board_graph").html("<%= escape_javascript(render 'graph') %>");
<% end %>